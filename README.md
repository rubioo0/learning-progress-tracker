# Learning Progress Tracker

Small tool I built to track my own progress while learning test automation - sessions, time spent, that kind of thing.

Node.js + Express backend, SQLite for storage, Tailwind for the UI. Can import/export progress as Excel files.

## Run it

    npm install
    npm start

(or just run `Start-QA-Road.cmd` on Windows)
